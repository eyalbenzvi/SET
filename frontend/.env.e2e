# Used by `vite build --mode e2e` / `vite preview --mode e2e`.
# Mirrors a GitHub Pages project-subpath deployment so the end-to-end tests
# exercise the same base-path handling as production.
VITE_BASE=/set/
VITE_API_BASE=http://127.0.0.1:8787
