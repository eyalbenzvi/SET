{
  "name": "@set/worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker + Durable Object hosting authoritative SET game rooms.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@set/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "wrangler": "^4.42.0"
  }
}
