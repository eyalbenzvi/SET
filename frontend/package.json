{
  "name": "@set/frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static, mobile-first SET game client for GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:e2e": "vite build --mode e2e",
    "preview:e2e": "vite preview --mode e2e"
  },
  "dependencies": {
    "@set/shared": "*"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
